{
  "name": "@the-garden/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contracts mirroring backend DTOs. Single source of truth for cross-boundary shapes.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "eslint": "^9.17.0"
  }
}
